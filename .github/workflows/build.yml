- name: Compilar APK en modo silencioso
      env:
        BUILDOZER_WARN_ON_ROOT: 0
        PLAY_STORE_BUILD: 0
      run: |
        export PATH=$PATH:~/.local/bin
        # Le agregamos la bandera de silencio total para que no sature la pantalla
        buildozer android debug quiet
